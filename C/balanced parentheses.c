#include <stdio.h>#include <stdlib.h>#define MAX_SIZE 101#include <string.h>char s2[100];int top=-1;void push(char x){   if(top == MAX_SIZE-1){       printf("Error! Stack Overflow!\n");   }       top++;       s2[top]=x; }void pop(){    if(top==-1){        printf("Empty Stack");    }    top--;}int ArePair(char opening,char closing){	if(opening == '(' && closing == ')') return 1;	else if(opening == '{' && closing == '}') return 1;	else if(opening == '[' && closing == ']') return 1;	return 0;}int checkBalance(char expression[]){for(int i=0;i<strlen(expression);i++){if(expression[i]=='(' || expression[i]=='{' || expression[i]=='['){push(expression[i]);        }        else if(expression[i]==')' || expression[i]== '}' || expression[i]== ']'){            if(top==-1 || ArePair(s2[top],expression[i])==0){                return 0;            }            else{                pop();            }        }    }    if(top==-1){        return 1;    }    else{        return 0;    }}int main(){    char s[100];    printf("Enter Expression: ");     scanf("%[^\n]%*c", s);int x = checkBalance(s);    if(x==1){        printf("Balanced");    }    else{        printf("Unbalanced");    }    return 0;}